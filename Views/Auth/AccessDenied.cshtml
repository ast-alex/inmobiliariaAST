@{
    var idUsuario = User.FindFirst("ID_usuario")?.Value;
}

<div class="access-denied-container text-center">
    <h2 class="text-danger">Acceso Denegado</h2>
    <p>No tienes permiso para ver esta página. Solo los <strong>ADMINISTRADORES</strong> tienen acceso.</p>
    <a href="@Url.Action("Detalles", "Usuario", new { id = idUsuario })" class="btn btn-primary">Ir a mi perfil</a>
    <a href="@Url.Action("Index", "Home")" class="btn btn-secondary">Volver al Home</a>
</div>
