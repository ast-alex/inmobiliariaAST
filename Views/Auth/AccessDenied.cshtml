@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="access-denied-container text-center">
    <h2 class="text-danger">Acceso Denegado</h2>
    <p>No tienes permiso para ver esta página. Solo los <strong>ADMINISTRADORES</strong> tienen acceso.</p>
    <a href="@Url.Action("Index", "Usuario")" class="btn btn-primary">Volver a la lista de usuarios</a>
</div>
